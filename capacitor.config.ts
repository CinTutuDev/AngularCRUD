import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Gym Registros',
  webDir: 'dist/gymangular-crud',
  bundledWebRuntime: false
};

export default config;
